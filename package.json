{
  "name": "orbitdb-field-manual",
  "version": "1.0.0",
  "description": "Readme TBD",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "markdownlint -c markdownlint.json *.md 0*/*.md",
    "build": "rm -rf dist; mkdir dist; pandoc -o dist/Book.pdf COVER.md */*.md"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "markdownlint": "^0.12.0",
    "markdownlint-cli": "^0.14.0"
  }
}
